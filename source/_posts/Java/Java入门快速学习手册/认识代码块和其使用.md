---
title: 认识代码块和其使用
author: 瓶子
categories:
  - - Java
    - Java快速入门学习手册
abbrlink: 
date: 2025-06-26 11:03:19
tags:
index_img:
banner_img:
---
本章主要介绍Java中代码块和代码块的使用注意事项。

<!-- more -->

## 一、代码块
> 代码块是类的5大成成员之一（成员变量，构造器，方法，内部类，代码块）

代码块分为两种:
* 静态代码块:
* * 格式:`static{}`
* * 特点:类加载时与类一起**优先执行**，由于类只会加载一次，所以静态代码块也只会执行一次。
* * 作用:完成类的初始化，例如:对静态变量的初始化赋值。

* 实例代码块:
* * 格式:`{}`
* * 特点:**每次创建对象都会执行**，并且在构造器前执行。
* * 作用:和构造器一样完成对象的初始化，例如:对实例变量的初始化赋值。

```java
public class Demo{
    static{ // 静态代码块
        String log = "静态代码块";
    }
    {   // 实例代码块
        System.out.println("实例代码块");     
    }
    
    public static void main(String[] args) {
        System.out.println(log);    // log已经在静态代码块中初始化，所以可以直接访问输出内容
        new Demo();     // 每次`new`一个对象都会执行实例代码块一次。
        new Demo();
    }
}
```

